# QubeTX release context

ND-300 publishes the `nd300` crate after a successful `main` CI run, then publishes GitHub binaries and installers from an immutable version tag. Releases include two binaries, six platform targets, four Windows installer variants, checksum sidecars, and provenance attestations.

The homepage is a separate repository and deployment. SD-300 and Shaughv OS pages remain delisted/WIP unless explicitly brought back into scope.
