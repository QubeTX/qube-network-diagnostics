TT;DR: On a future safe Mac, repeat read-only native ARM and Rosetta Intel smokes against quarantined public archives and inspect final diagnostic presentation.

## Why
The final parser pass added richer macOS connection, IPv6/process, DHCP, VPN, TLS/DNS, and networkQuality handling after earlier physical smokes. Fixtures and hosted builds cover correctness/trust, but a real Wi-Fi host offers richer presentation evidence.

## Plan
Run native ARM table/core JSON/technician JSON and Rosetta x86_64 from quarantined public archives. Inspect interface count/name, connected Wi-Fi metadata, listeners, IPv6 endpoints, DHCP normalization, VPN state, TLS evidence, and DNSSEC wording without publishing private values. Never run `fix`.

## Impact
Adds confidence in macOS 26 live output without mutating network state. It does not block release because both hosted architectures and public Gatekeeper checks remain authoritative.

## Acceptance
Output matches actual host state, both architectures terminate within caps, sensitive SSIDs remain unavailable, dormant tunnels are not classified as VPNs, and Gatekeeper reports Notarized Developer ID for both binaries.

## Verification
- [ ] Native ARM public archive smoke passes
- [ ] Rosetta Intel public archive smoke passes
- [ ] Live topology/presentation matches the physical host
- [ ] Gatekeeper reports Notarized Developer ID for both binaries

## Status
Deferred P1 at the operator's request; read-only and non-release-blocking.

## Activity
- 2026-07-17 10:18 — migrated ND36-010 to Backlog (agent: codex)
